# Документация

## Практическая документация

Описывает то, что **уже работает**. Колонка «Код» — файлы, которые правишь по
этому документу; ищи по ней, а не по всему дереву.

| Документ | О чём | Код |
|---|---|---|
| [API.md](API.md) | **Web API менеджера** для внешних приложений (mini-app, биллинг, бот, панель): ключи и scopes, все эндпоинты с примерами, приём платежей Stars | `webapi/*.py`, `webapi/dispatch.sh`, `lib/webapi.sh` |
| [DEMO-KEYS.md](DEMO-KEYS.md) | **Демо-ключи** (воронка, идея 13): `demo_create()`, таблица `DEMOS_DB`, enforcement капов кроном, грейс-сутки + авто-purge, показ по кластеру | `lib/demo.sh`, `lib/ui_users.sh` |
| [YOOMONEY.md](YOOMONEY.md) | **Приём рублей через ЮMoney**: продажа тарифов за рубли без платёжного провайдера (личный кошелёк, метка платежа, опрос истории) | `lib/yoomoney.sh`, `lib/tgbot_client.sh`, `lib/tgbot_menu.sh` |
| [NOTIFICATIONS.md](NOTIFICATIONS.md) | **Уведомления бота**: подписка (менеджер) и баланс (cibpn-webapp), кастомные эмодзи, канал-DM, как добавить новое уведомление | `lib/notify.sh`, `lib/tgbot*.sh` |
| [08-multiprotocol.md](08-multiprotocol.md) | **Мультипротокол**: VLESS+REALITY+XHTTP, SS-2022, Trojan/WS (Xray), TUIC v5 (sing-box) рядом с Hysteria, единая подписка | `lib/protocols.sh`, `lib/ui_protocols.sh` |
| [09-online-activity.md](09-online-activity.md) | **Онлайн-статус**: активность по всем протоколам, гистерезис, live-пуш через SSE | `lib/online.sh`, `lib/publish.sh`, `webapi/wa_online.py` |
| [10-multiprotocol-limits.md](10-multiprotocol-limits.md) | **Лимит скорости для всех протоколов** (Стадия 3): tc HTB+fq_codel не только для Hysteria | `lib/perf.sh`, `lib/ui_perf.sh` |

---

# Децентрализованный маркет локаций — проектные заметки

**Статус: исследование, не план работ.** Ничего из этого не реализовано и не согласовано.
Документы написаны, чтобы разложить идею по полкам и честно перечислить,
обо что она может разбиться. Читать в порядке нумерации.

## Суть задумки

Сегодня `cluster.sh` строится на общем секрете: кто знает `cluster_secret`, тот
полноправный член кластера и видит всех юзеров всех нод. Это нормально, пока все
ноды твои, и разваливается, как только появляется чужой владелец.

Идея — разрезать сегодняшнюю «ноду» на две роли с разными ключами:

* **Издатель (issuer)** — продал подписку юзеру. Владеет юзером: знает его,
  биллит, банит, задаёт квоту и тариф.
* **Оператор (operator)** — его сервер терминирует трафик. Про юзеров не знает
  ничего и знать не должен.

Вместо пароля юзер получает **ваучер** — подписанную издателем capability вида
«предъявитель обслуживается на ноде A до 18:00». Оператор проверяет подпись и
срок — и всё, никакой общей базы, никакой синхронизации юзеров. Нода физически
не может управлять чужими юзерами: у неё нет ни их данных, ни ключа издателя.
Издатель не может управлять чужой нодой: она сама решает, чьи подписи принимать.

Прямой аналог — роуминг в сотовой связи. Vodafone обслуживает абонента МТС, не
имея доступа к базе МТС, и выставляет счёт МТС, а не абоненту.

Оплата между издателем и оператором — стейблкоин на L2 плюс **предоплата с
тикающими расписками**. Ключевой принцип: **никто никому не даёт в долг больше
одного тика (~100 МБ)**. Из этого следует почти всё остальное, включая ответ на
вопрос «а что мешает наплодить юзеров и не платить»: ничего не мешает плодить,
но байты идут только против оплаченных расписок, поэтому количество юзеров
перестаёт быть значимой величиной.

## Два вывода, которые стоит держать в голове

**Блокчейн тут почти не нужен.** Задача «доказать полномочие, не отдавая власть
над юзером» — это подпись, а не консенсус. Цепь оправдана ровно в двух местах:
расчёты между незнакомцами и точка рандеву, за появление в которой платят (07).
Всё остальное — PKI и подписанные записи. Данные юзеров в цепь класть нельзя ни
при каком раскладе.

**Заимствовать чужую архитектуру целиком — ошибка, и она повторяется дважды.**
Цепь оптимизирована под согласие о порядке событий, а нам нужно доказательство
полномочия (01). P2P-сеть Bitcoin оптимизирована под то, чтобы каждый нашёл
всех, а нам нужно ровно обратное (07). Оба раза правильный ход — вычитание.

**Самые опасные проблемы — не технические.** Экономика и криптография у этой
системы сходятся (см. 03 и 04). А вот abuse-ответственность (`04-attacks.md`,
A-01), то, что мы продаём приватность, а обеспечивает её анонимный незнакомец
(A-02), и то, что каталог нод — это готовый блоклист (A-21), — это проблемы, у
которых честного решения пока нет. Все четыре красных — конфликты требований, а
не дефекты кода.

## Навигация

| Документ | О чём |
|---|---|
| [01-problem.md](01-problem.md) | Что не так с текущей моделью, чего хотим, почему блокчейн — не ответ на главный вопрос |
| [02-architecture.md](02-architecture.md) | Роли, ваучеры, подписка, gossip. Как ложится на `auth.type: command` |
| [03-economics.md](03-economics.md) | Кредит vs предоплата, каналы, расписки, размер тика. Ловушка общего escrow |
| [04-attacks.md](04-attacks.md) | **Каталог проблем и атак.** Самый важный документ |
| [05-examples.md](05-examples.md) | Конкретные форматы, псевдокод, сценарии с цифрами |
| [06-roadmap.md](06-roadmap.md) | Этапы, что делать первым, где точки невозврата |
| [07-discovery.md](07-discovery.md) | Как участники находят друг друга и почему модель Bitcoin здесь не подходит |

## Если читать только одно

`04-attacks.md`. Архитектура и экономика — это ремесло, они сойдутся. Вопрос
только в том, живёт ли эта штука с A-01 (abuse), A-02 (нода видит трафик),
A-11 (рынок лимонов) и A-21 (каталог нод = блоклист). Если ответ «нет» —
остальное неважно.
